# Memory V3 TODO

这个文件只记录 V3 的实现待办。

V3 的核心很简单：

- 规划一个独立工作区
- 只允许存放 `.md`
- 不预先规定内容、格式、命名
- 允许 agent 自由查询 / 修改 / 创建这些 Markdown

## 目标

- 给 agent 一个自由长期工作区
- 让 agent 自己决定如何记录经验
- 不把 V3 设计成固定 schema 系统

## 待办

### 1. 工作区目录

- 已确定目录名：`memory/global_memory/`
- 评估是否真的需要 `latest` 之类的辅助入口

### 2. 文件约束

- 已落地“只把 `.md` 视为合法工作区文件”
- 已允许子目录
- 决定是否需要对非 Markdown 临时文件做自动清理，而不只是标记 invalid

### 3. agent 能力

- 已通过 skill 和运行时边界允许 agent 查询已有 `.md`
- 已通过 skill 和运行时边界允许 agent 修改已有 `.md`
- 已通过 skill 和运行时边界允许 agent 创建新的 `.md`
- 继续保持“不要求固定模板”

### 4. 运行时边界

- 已把 V3 写成独立 skill
- 已明确 V3 与 V1 / V2 是并列关系
- 已允许在对局进行中实时读写
- 继续评估是否需要更显式的 prompt 注入策略

### 5. 前端与可视化

- 已支持前端展示 V3 工作区
- 已支持前端查看/搜索 `.md`
- 继续评估是否需要前端直接新建/编辑 `.md`

### 6. 剩余验收

- 工作区目录可创建
- invalid file 标记行为稳定
- agent 可自由读写 Markdown
- 不依赖固定 schema

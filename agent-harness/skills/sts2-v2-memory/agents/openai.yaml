interface:
  display_name: "STS2 V2 Memory"
  short_description: "Query past STS2 run experience"
  default_prompt: "Use $sts2-v2-memory to query experience from previous STS2 runs so the gameplay agent can make better decisions with evidence from `memory/archive/index.sqlite` and completed-run artifacts."
